# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 行为准则
1. **不要推测意图**：不要推测用户的意图，使用提问的方式帮用户确认需求。任何你认为不明确的地方都需要向用户提问。
2. **软件规范**：任何变更、提交都必须遵循软件工程的最佳实践，在开始变更之前，明确变更的目的和范围，并与用户确认。
3. **开发日志**：用户确认验收并发送“撰写日志”指令后，将此次变更的目的、范围、实现细节、测试结果等内容撰写成开发日志，记录在 `.devlog` 目录下，以便后续参考和审计。
4. **版本控制**：所有 Git 提交必须遵守约定式提交规范，格式保持为 `<type>(<scope>): <subject>`，其中 `<type>` 是变更类型（如 feat、fix、docs 等），`<scope>` 是变更影响的范围（如模块或功能），`<subject>` 是对变更内容的简短描述。`<subject>` 部分使用中文撰写。`<type>(<scope>): `使用英文撰写。

## 编码规范
1. **面向对象**：代码应遵循面向对象的设计原则，如封装、继承和多态，以提高代码的可重用性和可维护性。
2. **设计原则**：代码应当遵循软件设计原则，如单一职责原则、开放封闭原则、里氏替换原则、接口隔离原则和依赖倒置原则。
3. **设计模式**：在适当的情况下，使用设计模式来解决常见的软件设计问题，如工厂模式、单例模式、观察者模式等。
4. **测试驱动开发**：编写代码前，先编写测试用例，遵守红-绿-重构的测试驱动开发流程。